# 2026-08-31

## Reordered /majlis advanced sidebar (App\Livewire\Pages\Events\Index::form())

- Moved the **Tarikh** (date) section to the **top** of the filter sidebar.
- Folded the **Jenis majlis** (`event_category_ids`) field into the **Topik & rujukan**
  section (now its first field); the standalone `Jenis majlis` `Section` was deleted.
- New section order: Tarikh, Masa, Lokasi majlis, Bahasa, Cari dalam, Penceramah,
  Topik & rujukan, Untuk siapa, Format, Lokasi berdekatan (10 → 9 sections).
- The `event_category_ids` property, `buildSearchFilters()`, `defaultFilterData()`,
  `normalized*` and `fill*` methods are untouched — only the form schema layout moved, so
  saved searches / shared URLs / API keep working.
- `pint` clean; `EventSearchTest` 104 passed (no tests assert sidebar section order/count).
- Follow-up: moved the **Format** section up to sit directly under **Tarikh** (date).
  Final order: Tarikh, Format, Masa, Lokasi majlis, Bahasa, Cari dalam, Penceramah,
  Topik & rujukan, Untuk siapa, Lokasi berdekatan.

## Tarikh date shortcut (single Select) — replaced duplicate pickers

- Replaced the two standalone `starts_after`/`starts_before` DatePickers (in the old
  `Tarikh` Section) + the duplicate standalone `<section class="py-4">` quick-link anchors
  ("Hari ini"/"Hujung minggu") in `resources/views/livewire/pages/events/index.blade.php`
  with ONE `Select::make('date_shortcut')`: Semua | Hari ini | Esok | Minggu ini |
  Hujung minggu | Bulan ini | Minggu depan | Bulan depan | Julat tersuai.
  Matches the penceramah profile page pattern (`persons/show.blade.php` `flux:radio.group`).
- `date_shortcut` is **form-only** (NOT `#[Url]`-bound). The canonical range still travels as
  `starts_after`/`starts_before` so saved searches / shared URLs keep working.
- Named shortcut → day range resolved in `normalizedUrlState()`/`normalizedFilterData()` via
  `effectiveDateShortcut()` + `dateRangeForShortcut()` (uses `UserDateTimeFormatter::userNow()`
  in visitor TZ). `custom` defers to the two DatePickers, revealed by `visible()` when
  `date_shortcut === 'custom'`. `effectiveDateShortcut` infers `custom` when raw dates are
  present but shortcut is `all`/empty (preserves shared-URL ranges).
- The standalone blade `<section class="py-4">` + `$weekendQuery` were removed from the index
  blade. `weekendDateRange()` = Sat+Sun inclusive.
- `pint` clean. Live on https://ilmu360.test/majlis: all 9 options render, duplicate anchors gone.

## Test flakiness finding (IMPORTANT — not a real regression)

- `EventSearchTest` is **non-deterministic**: re-running with NO code changes gave
  10 failed → 11 failed → 1 failed → 104 passed across runs this session. Failures jumped
  between describe blocks (capacity / scopeEntry / gender filter) — classic test-ordering
  pollution, NOT a bug in the date-shortcut change. The earlier "capacity MissingAttributeException
  / scopeEntry undefined" was pollution noise, not caused by `Events/Index.php`.
- `VisitorCountryResolver` uses a static memo keyed on `app()+request` (`ensureCacheScope()`);
  it resets per HTTP request so it does NOT leak via the `applyDefaultCountryScope()` path
  (which runs inside a request). Likely other global state (DB/cache/config) is the real polluter.
- Action: do NOT chase the fluctuating failures as if they were date-shortcut regressions.
  If the suite must be stabilized, find the actual leaking test (DB rows / fakes / config / cache
  not reset between tests), not the date UI.
